/*
package frc.team6718.robot.subsystems;

import edu.wpi.first.wpilibj.AnalogInput;
import edu.wpi.first.wpilibj.AnalogTrigger;
import edu.wpi.first.wpilibj.Counter;
import edu.wpi.first.wpilibj.Spark;
import edu.wpi.first.wpilibj.command.Subsystem;
import frc.team6718.robot.RobotMap;

public class GripperSubsystem extends Subsystem {
    private Spark gripper;
    private AnalogInput gripperHallSensor;
    private AnalogTrigger gripperSensorTrig;
    private Counter gripperCounter;


    public GripperSubsystem() {
        super("Gripper");

        gripper = new Spark(RobotMap.GRIPPER_MOTOR);
        gripperHallSensor = new AnalogInput(RobotMap.GRIPPER_MOTOR_HALL);
        gripperSensorTrig = new AnalogTrigger(gripperHallSensor);
        gripperCounter = new Counter(Counter.Mode.kExternalDirection);
        gripperCounter.
    }

    @Override
    protected void initDefaultCommand() {

    }
}
*/
